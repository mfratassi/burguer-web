@model HomeViewModel

@await Html.PartialAsync("_Carousel")

<h2>Best Choices</h2>
<h4>Pick your best</h4>

<div class="row">
    @foreach (Snack snack in Model.Starred)
    {
        @Html.Partial("_SnackSummary", snack);
    }
</div>